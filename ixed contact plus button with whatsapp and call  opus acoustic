<div class="opus-fab-container">
    <div class="opus-menu-stack" id="opusMenu">
        <div class="opus-btn-wrapper">
            <span class="opus-tooltip">WhatsApp Us</span>
            <a href="https://wa.me/971508265672?text=Hello!%20I%20would%20like%20to%20inquire%20about%20your%20services." 
               target="_blank" class="opus-btn glass-wa pulse-wa">
                <i class="fab fa-whatsapp"></i>
            </a>
        </div>

        <div class="opus-btn-wrapper">
            <span class="opus-tooltip">Call Support</span>
            <a href="tel:+971508265672" class="opus-btn glass-brand pulse-brand">
                <i class="fas fa-phone-alt"></i>
            </a>
        </div>
    </div>

    <div class="opus-btn-wrapper">
        <span class="opus-tooltip">Contact Us</span>
        <button class="opus-btn-main glass-brand pulse-brand" id="opusToggle">
            <i class="fas fa-plus opus-icon-plus"></i>
        </button>
    </div>
</div>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<style>
:root {
    /* Deep Modern Colors */
    --brand-blue: #2988c0;
    --wa-glitter: linear-gradient(135deg, #075e54 0%, #128c7e 50%, #25d366 100%);
    --animation-speed: 2.5s;
}

.opus-fab-container {
    position: fixed;
    bottom: 30px;
    left: 30px;
    z-index: 999999;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
}

.opus-btn-wrapper {
    position: relative;
    display: flex;
    align-items: center;
}

/* Tooltip - Sleek & Dark */
.opus-tooltip {
    position: absolute;
    left: 90px;
    background: rgba(17, 17, 17, 0.9);
    backdrop-filter: blur(5px);
    color: #fff;
    padding: 6px 14px;
    border-radius: 20px;
    font-size: 13px;
    font-weight: 500;
    white-space: nowrap;
    opacity: 0;
    transform: translateX(-10px);
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
}

.opus-btn-wrapper:hover .opus-tooltip {
    opacity: 1;
    transform: translateX(0);
}

/* Button Base - No Borders */
.opus-btn, .opus-btn-main {
    position: relative;
    border: none;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    color: white;
    display: flex;
    justify-content: center;
    align-items: center;
    cursor: pointer;
    border-radius: 50%;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

/* Glow Shadows instead of Borders */
.opus-btn-main, .glass-brand {
    background: var(--brand-blue);
    box-shadow: 0 8px 25px rgba(41, 136, 192, 0.5);
}

/* Glittering WhatsApp Style */
.glass-wa {
    background: var(--wa-glitter);
    background-size: 200% 200%;
    animation: glitter-flow 3s infinite linear;
    box-shadow: 0 8px 25px rgba(18, 140, 126, 0.5);
}

.opus-btn-main { width: 72px; height: 72px; font-size: 28px; z-index: 10; }
.opus-btn { width: 66px; height: 66px; text-decoration: none; font-size: 26px; }

/* Menu Stack Animation */
.opus-menu-stack {
    display: flex;
    flex-direction: column;
    gap: 18px;
    margin-bottom: 25px;
    opacity: 0;
    pointer-events: none;
    transform: translateY(30px) scale(0.5);
    transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.is-active .opus-menu-stack {
    opacity: 1;
    pointer-events: auto;
    transform: translateY(0) scale(1);
}

/* High-Quality Perpetual Pulse */
.pulse-brand::after, .pulse-wa::after {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 50%;
    z-index: -1;
}

.pulse-brand::after {
    background: var(--brand-blue);
    animation: perpetual-pulse var(--animation-speed) infinite;
}

.pulse-wa::after {
    background: #128c7e;
    animation: perpetual-pulse var(--animation-speed) infinite;
}

@keyframes perpetual-pulse {
    0% { transform: scale(1); opacity: 0.6; }
    100% { transform: scale(1.8); opacity: 0; }
}

@keyframes glitter-flow {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

/* Icon Rotation */
.opus-icon-plus { transition: transform 0.5s ease; }
.is-active .opus-icon-plus { transform: rotate(135deg); }

/* Hover Scale */
.opus-btn:hover, .opus-btn-main:hover {
    transform: scale(1.1);
    filter: brightness(1.2);
}
</style>

<script>
(function() {
    const container = document.querySelector('.opus-fab-container');
    const toggle = document.getElementById('opusToggle');
    toggle.addEventListener('click', (e) => {
        e.stopPropagation();
        container.classList.toggle('is-active');
    });
    document.addEventListener('click', (e) => {
        if (!container.contains(e.target)) container.classList.remove('is-active');
    });
})();
</script>
